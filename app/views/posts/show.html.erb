<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @post.title %>
  <p class="text-left">Submitted <%=	 time_ago_in_words(@post.created_at) %> ago. <%= link_to 'Edit', edit_post_path(@post) %> |
  <%= link_to 'Destroy', @post, method: :delete, data: { confirm: 'Are you sure?' } %>
</p>

<p>

</p>

<p>
  <strong>Body:</strong>
  <%= @post.body.html_safe %>
</p>

<%= link_to 'Back', posts_path %>
